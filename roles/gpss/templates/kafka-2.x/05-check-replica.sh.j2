{% for k in kafka.jobs %}
{% if k.topic_name is defined -%}
   # {{ k.topic_name }}
{% endif %}
/usr/local/kafka/bin/kafka-topics.sh --describe --zookeeper {{ kafka.zookeeper_hosts }}/gpdb-kafka --topic {{ k.topic_name }}
{%- endfor %}
